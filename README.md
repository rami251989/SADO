# 🔥 SADO – Smart Excel Data Analyzer & BI Dashboard

منصة ويب ذكية لتحليل وتنظيف بيانات Excel وبناء لوحات تحكم تفاعلية **شبه Power BI**  
(مبنية باستخدام **Python + Streamlit**، وتدعم اللغة العربية بشكل كامل).

---

## ✨ المزايا الرئيسية

### 1️⃣ رفع ومعاينة البيانات
- رفع ملفات **Excel** (`.xlsx`, `.xls`).
- عرض أول 100 صف من البيانات بشكل تفاعلي.
- كروت إحصائية سريعة (KPI Cards):
  - عدد الصفوف.
  - عدد الأعمدة.
  - حجم الذاكرة.
  - نسبة الصفوف التي تحتوي على قيم مفقودة NaN.

---

### 2️⃣ تنظيف البيانات (Data Cleaning)

#### 🧹 عمليات عامة على مستوى الجدول
- حذف الصفوف المكررة.
- حذف الصفوف الفارغة كليًا.
- تعبئة جميع قيم NaN بقيمة ثابتة يختارها المستخدم.

#### ✨ تنظيف عمود واحد (Column-level Cleaning)
لكل عمود يمكنك:

- Trim (إزالة المسافات من البداية والنهاية).
- Normalize Spaces (تنسيق المسافات داخل النص).
- تحويل إلى:
  - lower (حروف صغيرة)
  - UPPER (حروف كبيرة)
  - Title Case
- إزالة رموز معينة تحددها أنت.
- إزالة الأرقام فقط أو الإبقاء على الأرقام فقط.
- الإبقاء على الحروف العربية والإنجليزية فقط.
- استبدال قيمة معينة بقيمة أخرى (Replace).
- التعامل مع القيم المفقودة NaN:
  - حذف الصفوف التي تحتوي NaN في هذا العمود.
  - تعبئة NaN بقيمة ثابتة.
  - تعبئة NaN بالمتوسط (mean).
  - تعبئة NaN بالوسيط (median).
  - تعبئة NaN بالـ mode.

#### 🔄 تغيير نوع الأعمدة (Dtype Casting)
- تحويل العمود إلى:
  - `string`
  - `int`
  - `float`
  - `bool`
  - `datetime`

#### ✏️ إعادة تسمية الأعمدة
- واجهة بسيطة لإعادة تسمية كل عمود لاسم أوضح وأسهل في التحليل.

#### 🔗 دمج الأعمدة (Column Merge)
- دمج أكثر من عمود في عمود واحد جديد بطريقتين:
  - دمج نصي (concat) بفاصل تختاره (مثال: `" - "`).
  - جمع رقمي (sum) لأكثر من عمود في عمود واحد.

---

### 3️⃣ لوحة تحكم تفاعلية (BI Dashboard)

لوحة تحكم تشبه أدوات الـ BI (مثل Power BI) مع:

#### 🎛 فلترة البيانات (Slicer)
- اختيار عمود فلترة.
- اختيار نوع الشرط:
  - `=`
  - `contains`
  - `>`
  - `<`
- قيمة الشرط.
- تطبيق الفلتر على جميع الرسوم في لوحة التحكم.

#### 📊 أنواع الرسوم المتاحة
يمكنك إنشاء أكثر من عنصر Dashboard، كل واحد بعدة إعدادات:

- **Bar Chart** – فئة مقابل قيمة (Category vs Value) مع تجميع:
  - sum / mean / count
- **Line Chart** – Trend حسب X.
- **Pie Chart** – نسب مئوية حسب فئة.
- **Scatter Plot** – X مقابل Y (علاقات بين متغيرين).
- **Histogram** – توزيع متغير رقمي.
- **KPI Card** – قيمة واحدة (Sum / Mean / Count).

#### 🗂 قوالب Dashboard (Templates)
- حفظ إعدادات لوحة التحكم (الرسوم + الفلتر) كملف JSON.
- تحميل القالب مرة أخرى لإعادة بناء نفس لوحة التحكم بنقرة واحدة.

---

### 4️⃣ التقرير الإحصائي المتقدم (Advanced EDA Report)

نظام تقرير قوي جدًا لتحليل البيانات:

#### محتوى التقرير:
- معلومات أساسية (عدد الصفوف، الأعمدة، الحجم، أنواع الأعمدة).
- Missing Data Analysis:
  - عدد القيم المفقودة لكل عمود.
  - نسبة المفقود لكل عمود.
  - الأعمدة الفارغة بالكامل.
- Unique Values:
  - عدد القيم الفريدة لكل عمود.
  - Top 10 values لكل عمود.
- Numeric Analysis:
  - describe() كامل (count, mean, std, min, quartiles, max).
  - Skewness (الالتواء).
  - Kurtosis (التفلطح).
  - Outliers باستخدام IQR.
- Correlation:
  - مصفوفة الارتباط بين المتغيرات الرقمية.
  - اكتشاف العلاقات القوية (|corr| ≥ 0.7 أو 0.85).
- Text Columns:
  - متوسط طول النص.
  - أطول وأقصر نص.
- ML Readiness:
  - الأعمدة التي تحتاج Encoding.
  - الأعمدة التي تحتاج Scaling.
- Intelligent Insights:
  - أعمدة فيها Missing عالي.
  - أعمدة تبدو كـ ID.
  - أعمدة ذات توزيع ملتوي skew.
  - مشاكل Multicollinearity.
  - أعمدة تصنيفية عالية التعدد.

#### 📄 إخراج PDF احترافي
- صفحة غلاف جميلة للتقرير.
- نص التقرير كاملاً.
- رسم:
  - Missing per column bar chart.
  - Correlation heatmap.
  - Histograms للأعمدة الرقمية.

---

### 5️⃣ دعم اللغة العربية

- الواجهة تدعم العربي والإنجليزي.
- استخدام `arabic-reshaper` و `python-bidi` لعرض العربي بشكل صحيح في الرسوم وملف PDF.
- تصميم Dark Theme مع لمسات ذهبية (gold) وBlue حديثة.

---

## 🛠️ التقنيات المستخدمة

- **Python 3.13**
- **Streamlit** – لواجهة الويب.
- **Pandas, NumPy** – لمعالجة البيانات.
- **Matplotlib** – للرسم والتحليل البصري.
- **arabic-reshaper, python-bidi** – لدعم اللغة العربية في الرسوم.
- **OpenPyXL** – للتعامل مع ملفات Excel.

---

## 🚀 كيف تشغّل المشروع محليًا

### 1️⃣ استنساخ المشروع (Clone)

```bash
git clone https://github.com/rami251989/SADO.git
cd SADO
